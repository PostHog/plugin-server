const { Client } = require('pg')

// Make sure clickhouse test database is in place
beforeAll(() => {
    // TODO: setup posthog_test database
})

// And that is truncated before each test
beforeEach(() => {
    // TODO: truncate posthog_test database
})

// Make sure postgres test database is in place
beforeAll(() => {
    // TODO: setup test_posthog database
})

// And that is truncated before each test
beforeEach(() => {
    // TODO: truncate test_posthog database
})
